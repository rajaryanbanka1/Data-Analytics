📌 Overview

The files in this folder represent cleaned and transformed datasets generated from the raw data using SQL and Python pipelines.

⚠️ Important:
The CSV files included here are sample extracts only, not the full dataset.

🎯 Why Only a Sample?

The original dataset (from Kaggle) contains 1000+ rows.
To keep the repository:

Lightweight
Easy to review
GitHub-friendly

I have included only a subset (~20–50 rows) of the cleaned data.
